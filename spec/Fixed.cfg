\* PREUVE : protocole CORRIGE (vol serialise par meta-verrou + re-verif peremption).
\* TLC DOIT prouver MutualExclusion (aucun contre-exemple sur tout l'espace d'etats).
SPECIFICATION Spec
CONSTANTS
  Procs = {p1, p2, p3}
  Fixed = TRUE
  NoOwner = NoOwner
INVARIANT MutualExclusion
