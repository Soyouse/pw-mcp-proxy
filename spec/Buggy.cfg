\* NEGATIVE-CHECK (anti-spec-creuse) : protocole ACTUEL (vol par unlink inconditionnel).
\* TLC DOIT trouver un contre-exemple a MutualExclusion. Si VERT ici => la spec est creuse.
SPECIFICATION Spec
CONSTANTS
  Procs = {p1, p2, p3}
  Fixed = FALSE
  NoOwner = NoOwner
INVARIANT MutualExclusion
